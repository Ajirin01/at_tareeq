import 'package:flutter/material.dart';

class InfoComment extends StatelessWidget {
  const InfoComment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
