import 'package:flutter/material.dart';

class ListMenuLayout extends StatelessWidget {
  const ListMenuLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
